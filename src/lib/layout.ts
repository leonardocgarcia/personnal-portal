// Shared width tokens so the "page shell" (header, footer, hero, list
// views) and the narrower "reading column" (article body, forms) stay
// consistent across pages instead of each one picking its own max-w-*.
export const SHELL = "mx-auto w-full max-w-5xl px-6 sm:px-8";
export const READ = "mx-auto w-full max-w-2xl px-6 sm:px-8";
